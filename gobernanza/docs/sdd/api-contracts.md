## Registro de cambios del documento

| Versión | Fecha | Descripción del cambio |
| --- | --- | --- |
| 1.0 | Julio 2026 | Creación inicial del documento |

## Endpoints completados automáticamente (MDD §4)

| Método | Ruta | Descripción | Auth | Notas |
|--------|------|-------------|------|-------|
| GET | `/health` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/groups` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/groups` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/groups/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| PUT | `/groups/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| DELETE | `/groups/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/participants` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| PUT | `/participants/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| DELETE | `/participants/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/tickets` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/tickets` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/tickets/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| DELETE | `/tickets/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/tickets/{id}/participants` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| DELETE | `/tickets/{id}/participants/{participantId}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/products` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| PUT | `/products/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| DELETE | `/products/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/tickets/{ticketId}/assignments` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/assignments` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/assignments/shared` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| DELETE | `/assignments/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| PUT | `/tickets/{ticketId}/tip` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| PUT | `/tickets/{ticketId}/participants/{participantId}/tip` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/tickets/{ticketId}/summary` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/tickets/{ticketId}/calculate` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/history` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/history/{id}` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/ocr` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/ai/parse-ticket` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/tickets/process` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| POST | `/api/v1/tickets/process` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |
| GET | `/api/v1/tickets/{ticketId}/summary` | (completado automáticamente — ampliar schemas) | Bearer | MDD §4 |